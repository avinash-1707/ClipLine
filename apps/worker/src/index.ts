// BullMQ consumer process. Queue definitions and processors arrive with the
// ingest and render units; this entry point only verifies the process boots.
console.log("worker booted (no queues registered yet)");
